// https://safe-marmot-26.clerk.accounts.dev

const authConfig = {
  providers: [
    {
      domain: "https://safe-marmot-26.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
};

export default authConfig;
